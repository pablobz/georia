<!DOCTYPE html>
<html lang="es" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/assets/css/global.css">
  </head>
  <body>

  <nav>
    <a class="linkhome" href="/">Georia</a><br>
    &#8627; {{ title }}
  </nav>

<br><br>

{%- for unidad in collections.unidad | sort(attribute="data.orden") -%}
  {%- if unidad.data.curso == codigo -%}
  <a href="{{ unidad.url }}">{{ unidad.data.title }}</a><br>
  {%- endif -%}
{%- endfor -%}

  </body>
</html>
